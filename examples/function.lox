
fun gauss(n) {
  var sum = 0;
  for (var i=1; i<=n; i=i+1) {
    sum = sum + i;
  }
  return sum;
}

fun sayHi(first, last) {
  print "Hi, " + first + " " + last + "!";
}

sayHi("Dear", "Reader");
print gauss(100);

